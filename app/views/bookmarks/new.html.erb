<h2>Add a new Bookmark</h2>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>

  <%= f.button :submit %>
<% end %>

<%= link_to 'Back', list_path(@list) %>
